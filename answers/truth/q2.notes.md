# q2 패킷 레벨 정밀 재분석 노트 (2026-09-02)

truth v1 → v2 정정의 근거. 분석은 로컬 raw(zeek 45M + suricata 31M + `extract_files/` 카빙 바이너리)로 수행.

## 호스트 인벤토리 (dhcp.log)
| IP | MAC | 호스트명 | 판정 |
|---|---|---|---|
| 192.168.0.53 | 00:60:6e:00:9d:f9 | password-ned-xp (WinXP/MSIE8) | **감염** |
| 192.168.0.54 | ec:f4:bb:4f:b0:96 | Dell-Dator32 | Wajam 애드웨어(PUP) |
| 192.168.0.51 | ec:f4:bb:4f:b2:45 | homer-xubuntu | 정상 (POP3/1und1 + 구글) |
| 192.168.0.2 | ec:f4:bb:4f:c0:d7 | (웹서버) | 공격 표적, 침해 없음 |
| 192.168.0.1 | 78:54:2e:9f:10:28 | 게이트웨이/DNS | 인프라 |

## .53 감염 채널 4개 (conn/http 실측)
1. **94.242.254.208:80 — 죽은 주 C2 비콘**: 773 conn, 전부 S0(무응답)·0바이트, median 13.07s 간격, 23.9h 지속.
   DNS 역추적 0건 = 하드코딩 IP. v1 truth·모든 보고서에 없던 IOC.
2. **136.243.24.249:80 — 살아있는 클릭사기 C2/TDS**: 117 req. Host 이중성 — `searchl.org`(가짜검색, 302 redirect)
   58건 + bare-IP(base64 경로 240~684자, 200) 59건. **searchl.org 는 dns.log 에 질의 0건** — 직결 + Host 헤더로만
   존재 (→ observed_iocs 가 http host 를 안 봐서 그라운딩이 기각하던 갭의 실증).
3. **88.214.241.199 — 과금 종단**: `/click?sid=<sha1>&cid=0` ×13, 전부 301, referer=searchl.org/search.
   Suricata `ET MALWARE Poweliks Clickfraud CnC M4`.
4. **46.108.156.146 (.gq TDS 홉)**: 3 req, 워드샐러드 경로 `/sullen/39508/sleepy/...`, Host 에 랜덤포트,
   referer=`2hood.eu/1/rx.php`(역시 DNS 무질의), 전부 404.

감염 유입 정황: `0a0a.eu`(198.37.112.45) 스캠 랜딩(`/1/index.php` 등 48 req) → searchl.org 체인.
죽은 DGA: 워드샐러드 .org 6종(webserviceline/onlineservice2013/serviceonlinetech/techsonlineervice/
onlineservicetest/webserviceline2013) 반복 질의, **전부 SERVFAIL**(도메인 사망) — C2 인프라 상당수가
캡처 시점에 이미 무력화된 상태였고 클릭사기 체인만 가동 중.

## pwned.se — 터널 아님 (v1 정정 근거)
- 1,045건 **전부 NXDOMAIN**, 응답자는 전부 내부 GW(192.168.0.1). 터널은 권위서버 응답으로 데이터를
  나르므로 전-NXDOMAIN 이면 **채널이 존재한 적이 없다.**
- **비감염 호스트도 질의**: .51 180건·.54 75건 — 전부 `wpad`/`isatap`/자기호스트명 (Windows 접미사 동작).
- .53 의 "터널스러운" 질의 = 바이너리 단일라벨 ~8종(각 30~64회, 23.4h) + 워드샐러드 .org 에 접미사가
  붙은 형태. **bare 바이너리 질의 0건** = Windows 리졸버의 검색접미사 자동부착.
- `password-ned-xp.pwned.se` = 자기 FQDN 해석(정상 동작).
→ pwned.se 는 이 LAN 의 DHCP 도메인 접미사. IOC 아님(차단해도 무해하나 "터널 유출"은 사실 아님).

## .2 웹공격 — 전건 시도 실패 (확정 근거)
- 인바운드 http 873건 / 7출처: 146.52.78.242(804), 66.135.38.206(32), 60.250.33.201(31),
  91.217.90.49(2, /rom-0), 98.100.15.218(2), 61.240.144.64(1, Masscan), 185.25.151.159(1).
- 응답: 404×796, 무응답×62, 403×4, 400×4. **200 은 7건뿐 — 전부 `GET /`(1041B 홈페이지)/`HEAD /`.**
- 페이로드가 지정한 다운로드 서버 6곳(54.248.244.82×802, 109.228.25.87×124, ubimed.com×64,
  github/google/testp3.pospr.waw.pl): **.2 의 conn 0건, .2 의 DNS 질의는 캡처 전체 0건.**
  .2 아웃바운드는 NTP 4곳 + 포트10 단발 아티팩트뿐.

## 해시 2개 — 바이너리 검증으로 정상 확정
- `a8ab7f86…` (446,592B): MZ + **VeriSign Class 3 코드서명, 'Nvidia Corporation'** 문자열,
  8.36.120.225(NVIDIA CDN)發, .54 수신. 정품 드라이버.
- `6a34b09d…` (2,222B): CWS(zlib SWF) → `MessageSenderV2_fla` XMLSocket 위젯,
  o.aolcdn.com(195.12.232.201, AOL CDN)發, referer aol.com. 광고테크 정품.

## 파이프라인 개선 거리 (이 분석이 발견)
1. `observed_iocs` 에 http Host 도메인 포함 (searchl.org 기각 갭) — **수정됨**
2. `BENIGN_SERVING` 에 nvidia/aolcdn (해시 오탐) — **수정됨**
3. S0 죽은-비콘(무응답 반복 직결) IOC 승격 — TODO (임계값 설계 + 교차 케이스 검증 필요)
